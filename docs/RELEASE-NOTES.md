# Release notes

## Production-hardened handoff

This ZIP is safer to deploy than the original upload, but it should still be treated as a production candidate until you run the verification commands and configure Supabase/Vercel secrets.

Key changes are documented in `PRODUCTION-SETUP.md`.
